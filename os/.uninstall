local termOrMonitor = cosUtils.termOrMonitor
local del = cosUtils.del

cosUtils.resetScreen(termOrMonitor)

del("os", true)
del("apis", true)
del("programReplacements", true)
del("startup", true)
del(".menu", false)
del(".settings", false)
del("back", false)

os.sleep(1.5)
shell.run("reboot")